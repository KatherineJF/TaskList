<h1> Edit a Task!</h1>
<h1>
  <%= edit_task_path(@task.id) %>
</h1>

<%= form_for @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit "Save Task" %>
<% end %>
